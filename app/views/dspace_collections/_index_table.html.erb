<table width="100%" class="table table-hover index-table">
  <thead>
    <tr>
      <th></th>
      <th width="10%"></th>
    </tr>
  </thead>

  <tbody>
    <% dspace_collections.each do |dspace_collection| %>
      <tr>
        <td>
          <%= link_to dspace_collection.name,
                  dspace_collection_path(dspace_collection.id),
                  title: dspace_collection.name,
                  class: 'btn btn-default object-name-btn' %>
        </td>

        <td class="action-btns-field">
          <%= link_to edit_dspace_collection_path(dspace_collection.id),
                  title: 'Edit',
                  class: 'btn btn-primary' do %>
                  <span class="glyphicon glyphicon-edit"></span>
          <% end %>
          <%= link_to dspace_collection_path(dspace_collection.id),
                  title: 'Destroy',
                  method: :delete,
                  data: { confirm: 'Are you sure?' },
                  class: 'btn btn-danger' do %>
                  <span class="glyphicon glyphicon-remove"></span>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
