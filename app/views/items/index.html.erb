<h1>Items</h1>
<ul>

<%= form_tag(items_path, :method => "get", id: "search-form") do %>
  <%= text_field_tag :search, params[:search], placeholder: "Título" %>
  <%= submit_tag "Buscar" %>
<% end %>
</br>

<% @items.each do |item| %>
  <li>
    <%= link_to item.name, item_path(item.id) %>
  </li>
<% end %>
</ul>
